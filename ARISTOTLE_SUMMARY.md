# Summary of changes for run 7e050fc0-c063-416d-b1ac-f736b524cd7e
FINAL BRIDGE REPAIR PASS — the two formalisation-coverage gaps are closed by a strictly append-only layer; both banked compilers (graph/path and direct formula-tree) are untouched, and the only edit to an existing file is four appended `import` lines in the aggregator `RequestProject/AlgebraicComplexity/VNP1Char2.lean`.

New modules under `RequestProject/AlgebraicComplexity/VNP1Char2/`:

- `PFamilyDegree.lean` (Repair 1 — standard p-family total-degree condition). Defines `PolyDegreeBounded` and `IsPFamily` (polynomially many variables *and* polynomially bounded total degree) without redefining or breaking `PolyFamily`. Proves `Formula.totalDegree_eval_le` (`deg f ≤ size f`), hence `VPe.isPFamily`; `AffineHypercubeRep.totalDegree_value_le` (a product of `M` affine forms has degree ≤ `M`, and Boolean-hypercube summation does not raise the maximal degree), hence `VNP1.isPFamily`, `VNP1LE3.isPFamily`, `VNP1LE2.isPFamily` — covering every family used in the reverse containment `VNP₁^{[≤2]} ⊆ N(VP_e)`; and `totalDegree_aeval_le` (degree-≤1 substitutions do not raise degree), hence `NondetClosure.isPFamily` and `nondetVPe_isPFamily`. The main result is restricted back to the standard literature universe by `nondetVPe_eq_VNP1LE2_standard` together with `nondetVPe_iff_VNP1LE2_isPFamily`, `nondetVPe_subset_VNP1LE2_standard`, `VNP1LE2_subset_nondetVPe_standard` and `VPe_subset_VNP1LE2_standard`; since membership in either class already implies `IsPFamily`, this is a certification, not a weakening.
- `BIZWPlus.lean` (Repair 2 — literal `w+` bridge). The pre-existing `VNP1BIZ` is left unrenamed and unmodified; the new class `VNP1BIZWPlus` requires of every factor *both* total degree ≤ 1 *and* variable support (`vars.card`) ≤ 2. Proves the requested `VNP1LE2_subset_VNP1BIZWPlus` (`VNP₁^{[≤2]}(F) ⊆ VNP₁,BIZ-w+(F)`), consuming the already-certified syntactic `SupportLE 2` theorem and bridging it to the semantic condition through the new `AffineForm.toPoly_vars_subset` / `toPoly_vars_card_le`; also the forgetful `VNP1BIZWPlus.toVNP1BIZ`, plus `nondetVPe_subset_VNP1BIZWPlus`, `VPe_subset_VNP1BIZWPlus` and the standard-p-family form. No external BIZ or Valiant statement is imported or axiomatized.
- `BridgeRepairAudit.lean` — non-vacuity (a concrete family lands in the new class and is a standard p-family), the firewall `totalDegree_one_not_supportTwo` proving that degree ≤ 1 does not imply support ≤ 2 (so the new class is strictly stronger and the bridge really carries support information), the field-scope marker, and `#print axioms` on all 29 new statements (all report only `propext`, `Classical.choice`, `Quot.sound`).
- `BridgeRepairOwners.lean` — append-only status entry.

Repair 3 (field-scope hygiene): inspection found no Lean statement using type equality or inequality with the two-element field; the only occurrence is a ring-*isomorphism* statement recorded for completeness, and the canonical positive hypothesis remains `∃ τ : F, τ ≠ 0 ∧ τ ≠ 1`, with finite-cardinality forms as corollaries only. No mathematical change was made; this is recorded as manuscript wording only.

Repair 4 (audit and artifact sync): full `lake build` completes successfully (8075 jobs, no errors, no warnings from the new modules); the new files contain no `sorry`, `admit`, `axiom`, `native_decide`, `unsafe`, `opaque` or `@[implemented_by]`; the existing direct-proof independence audit is preserved and still passes, and no banked proof was altered. The new report `VNP1_CHAR2_FINAL_BRIDGE_REPAIR_REPORT.md` lists the exact new theorem names, the dependency graph, both repairs, the build/axiom/placeholder results, the frozen-compiler check, the git commit of the repaired package and SHA-256 hashes of the changed files, and notes explicitly that any previous manuscript artifact hash is stale. No archive was produced, so no archive hash is quoted. No new novelty, P-vs-NP, VP-vs-VNP or runtime claim is made.

# Summary of changes for run e2717334-9a54-4e5d-8b35-f0f8a2a89a84
I added a second, fully independent, machine-checked proof of the characteristic-two support-two compiler, appended to the existing project without touching any banked declaration (the only edit to an existing file is seven `import` lines appended to the aggregator `RequestProject/AlgebraicComplexity/VNP1Char2.lean`).

New modules under `RequestProject/AlgebraicComplexity/VNP1Char2/`:

- `DirectBranchGadget.lean` — the one-bit six-factor branch gadget. For Boolean `a, b, c` over a field of characteristic two with `τ ≠ 0, 1` and `κ = τ(τ+1)`, `∑_{h∈{0,1}} Γ_τ(a,b,c;h) = (1+a+b+c)(1+ab)` (`branch_identity`), together with the explicit truth table (`branch_truth_table`: value `1` exactly on `(0,0,0), (1,0,1), (0,1,1)`, `0` on the other five states) and the proof that each of the six affine factors mentions at most two variables (`h`, `(c,h)`, `(c,h)`, `(b,h)`, `(a,h)`, `(a,b)`).
- `DirectFormulaCompiler.lean` — the direct formula-tree activation compiler: one fresh activation bit per syntax node, a root pin, two equality factors per multiplication gate, the branch gadget per addition gate, and the banked one-bit three-factor `1 + xy` gadget per leaf. Auxiliary index types are built by structural recursion out of disjoint sums, so freshness is literal. The semantic theorem `condZ_spec` (`Z_f(0) = 1`, `Z_f(1) = f.eval`) is proved by structural induction, giving `directRep_value` as an identity of polynomials in the original variables; all cube flattening is proved (`sum_cube_sum`, `sum_cube_unit`, `sum_prod_split`, `sum_pinned`), never via the false step `(∑A)(∑B) = ∑AB`. Sizes are exact: `q = 2l+2a+u = 2s − u` and `M = 1+3l+6a+2u`, hence `q ≤ 2s`, `2M ≤ 9s` and `M ≤ 5s`. Main theorem: `Formula.has_supportTwo_representation_direct`.
- `DirectClassLevelTwo.lean` — the load-bearing direct forward containment `N(VP_e(F)) ⊆ VNP₁^{[≤2]}(F)` (`nondetVPe_subset_VNP1LE2_direct`), merging the outer witness cube and the compiler cube over their disjoint union; the internal class equality and the width-one bridge corollary are then obtained by reusing the already-proved reverse containment and bridge.
- `TwoPointWeights.lean` — the optional two-point interpolation identity explaining the `|F| > 2` threshold.
- `RegressionTestsDirect.lean` — machine-checked tests: the eight-state truth table and the three allowed states by `decide`; leaves, addition, multiplication and the mixed formulas `(x+y)*z`, `x+(y*z)`, `(x+1)*(y+z)`; compiled value equals formula evaluation; support ≤ 2; exact aux/factor counts and the general bounds instantiated; freshness for `(x+y)*(x+y)` (count `13 = 2·6+1`, so equal-shaped subformulas do not share bits); instances over `GF(4)`, `GF(8)`, `GF(16)`; and the scope firewall `branch_unrestricted_fails`, proving the branch identity is false for a non-Boolean parent value, so it is claimed only in its Boolean form.
- `AxiomAuditDirect.lean` — `#print axioms` on every principal new theorem (all report only `propext`, `Classical.choice`, `Quot.sound`) plus a machine-checked **independence firewall**: a transitive dependency scan that fails the build if the direct compiler theorem or the direct class containment ever uses `rep2_value`, `has_supportTwo_affineHypercubeRepresentation`, `graphRep2_value`, `toDag_value` or the old forward containment. It passes.
- `DirectCompilerOwners.lean` — append-only status entry.

`lake build` on the whole project completes successfully with no errors and no warnings from the new files; a search of the tree finds no `sorry`, `admit`, `axiom`, `native_decide`, `unsafe`, `opaque` or `@[implemented_by]` declaration. The report `VNP1_CHAR2_DIRECT_FORMULA_REPORT.md` records the status block, the theorem list and dependency graph, the independence audit, the regression tests and firewalls, and separates what is machine-checked here from the frozen theorems that are reused and from external literature (Valiant; Bringmann–Ikenmeyer–Zuiddam), which is neither formalized nor axiomatized. The layer is a representation theorem with no evaluator: no P-vs-NP, VP-vs-VNP or runtime consequence is claimed, and no novelty claim is made.

# Summary of changes for run 658945fb-ada1-4bb5-8943-18151321c6e4
I extended the frozen support-three layer with a complete, append-only support-two formalization, following the memo's required proof order. No existing file was modified except eleven appended `import` lines in the aggregator `RequestProject/AlgebraicComplexity/VNP1Char2.lean`.

New algebra (`VNP1Char2/TernaryIdentity.lean`, `SupportTwoGadget.lean`): with `κ = τ(τ+1)` over a field of characteristic two, `κ ≠ 0` exactly under `τ ≠ 0, τ ≠ 1`; the support-two gadget `∑_{h∈{0,1}} κ⁻¹(τ+1+h)(x+τ+h)(κy+τ+h) = 1 + xy` (one summed Boolean bit, three affine factors); and the ternary identity `∑_h κ⁻¹(τ+1+h)(a+τ+h)(b+τ+h)(c+τ+h) = (1+a+b+c) + κ⁻¹abc`. Both are ring identities with `x, y, a, b, c` arbitrary (only the auxiliary is Boolean), with `MvPolynomial` instances. The cubic error is kept explicit, and the firewall `cubic_error_not_formally_zero` proves `X₀X₁X₂(1+X₀X₁) ≠ 0`, so it is never removed formally.

Proof-order firewall (`CompilerTwo.lean`, `GraphCompilerTwo.lean`): the generic compiler has an unconditional value theorem with the errors visible, and a second theorem that removes them only under a pointwise hypothesis for each fixed Boolean selector assignment. At the graph level that hypothesis is discharged: at an internal vertex of total degree three, two of the three incident edges share a head or a tail (pigeonhole), so the same-head/same-tail exclusion factor is present, and `uvw(1+uv) = 0` for Boolean values kills the error; only then is the hypercube summed and the identity concluded in the original formula variables. Each gadget occurrence gets its own fresh auxiliary (index type `A ⊕ (T ⊕ K)`), and the flattening of all cubes is proved rather than assumed.

Main results: `Formula.has_supportTwo_affineHypercubeRepresentation` — every binary division-free arithmetic formula over a characteristic-two field with `τ ∉ {0,1}` has a Boolean-hypercube representation whose every affine factor has support at most two, with `q ≤ 26·size f` and `M ≤ 70·size f`. I then proved the memo's sharp graph identity `P = 4a` (`Formula.toDag_numPairs`), sharpening these to `q ≤ 14·size f` (the memo's constant) and `M ≤ 34·size f`. At the class level, `nondetVPe_eq_VNP1LE2` gives the internal equality `N(VP_e(F)) = VNP₁^{[≤2]}(F)` (with `∃τ` and finite-field forms), and the minimal literature bridge `VNP1LE2_subset_VNP1BIZ` places these families in a width-one-ABP hypercube class defined by total-degree-≤1 factors, avoiding reliance on the internal affine-term-list convention.

Honest deviations: the memo's coarse `M ≤ 68·size f` is replaced by the verified `70·size f` (and `34·size f` sharply); the memo's second sharp identity `D + B = 2a` is not proved, because the number of degree-three vertices is not a function of the leaf/add/mul counts alone. Gadget minimality is only partial — the achievement of `(q,m) = (1,3)` and a zero-auxiliary lower bound of two factors are machine-checked, while the full impossibility statements are not formalized and are not claimed.

Audits: `lake build` completes successfully with no errors and no warnings from the new modules; `#print axioms` on all 52 principal new theorems reports only `propext`, `Classical.choice`, `Quot.sound`; a search of the tree finds no `sorry`, `admit`, `axiom`, `native_decide`, `unsafe`, `opaque` or `@[implemented_by]` declaration (only the word "admitting" in docstrings). Seven negative firewall theorems pin the scope (τ = 0/1 break the gadget; support two fails for unrestricted ABP labels; auxiliary reuse changes the value; a degree-three flow factor really has support three). The layer is a representation theorem with no evaluator: no P-vs-NP, VP-vs-VNP or runtime consequence is claimed, and the literature synthesis (Valiant; Bringmann–Ikenmeyer–Zuiddam) is stated as prose only. Everything is recorded in `VNP1_CHAR2_SUPPORT_TWO_REPORT.md` and in the status file `RequestProject/AlgebraicComplexity/VNP1Char2/SupportTwoOwners.lean`.

# Summary of changes for run de154ead-deb9-4464-9fab-3e076328e90c
I added the missing class-level layer on top of the existing `VNP1Char2` kernel bank, strictly append-only: no existing file was modified, renamed, weakened or reproved, and the only change to an existing file is five `import` lines appended to the aggregator `RequestProject/AlgebraicComplexity/VNP1Char2.lean`.

New files (all sorry-free, no axioms, no `native_decide`/`unsafe`/`opaque`/`@[implemented_by]`):
- `RequestProject/AlgebraicComplexity/VNP1Char2/PFamily.lean` — a minimal p-family glossary: `PolyBounded p` (explicit bound `p n ≤ c·(n+1)^k`) with its closure properties (sum, scalar multiple, composition, monotonicity), `PolyFamily F` (the `n`-th member a polynomial in `Fin (nvars n)` variables, so "polynomially many variables" is structural), the internal classes `VPe`, `VNP1LE3` and unrestricted `VNP1`, plus the two translations used later (an affine form is a formula of size `4·numVars+1`; a list of formulas has a product formula) and reindexing of affine-product representations.
- `Nondeterminism.lean` — the witness substitution and the closure operator `NondetClosure C` mirroring BIZ Definition 2.2, `VNPe := NondetClosure VPe`, monotonicity and `C ⊆ N(C)`.
- `ClassLevel.lean` — the main theorems, proved for `[Field F] [CharP F 2]` with `τ ≠ 0`, `τ ≠ 1`: `nondetVPe_subset_VNP1LE3` (`N(VP_e) ⊆ VNP₁^{[≤3]}`, the load-bearing one, flattening the outer witness cube and the compiler's fresh cube over their disjoint union via an explicit equivalence, with cube dimension `p n + 44·s(q n)` and factor count `84·s(q n)` shown polynomial through composition of polynomial bounds), `VPe_subset_VNP1LE3` (`VP_e ⊆ VNP₁^{[≤3]}`), `VNP1LE3_subset_nondetVPe` (reverse containment, formalized, using support ≤ 3 to bound each factor's formula size by 13), the resulting internal equality `nondetVPe_eq_VNP1LE3`, `nondetVPe_subset_VNP1`, and the field corollaries (`∃ τ ≠ 0,1` canonical hypothesis, and `2 < Nat.card F` for finite fields via the existing corollary); also, for completeness only, that a two-element characteristic-two field is `ZMod 2`.
- `ClassLevelAudit.lean` — a non-vacuity check (a concrete family lies in `VP_e`, hence in `VNP₁^{[≤3]}`) and `#print axioms` on every principal theorem; all report only `propext`, `Classical.choice`, `Quot.sound`.
- `ClassLevelOwners.lean` — append-only status update of the open owners, with a marker theorem recording that the class statement is a representation statement with no evaluator.

`lake build` completes successfully (errors: none; only pre-existing linter warnings, none from the new files), and a search of the new layer finds no `sorry`/`admit`/`axiom` declarations — the four `axiom` hits are documentation text saying nothing is axiomatized.

The report `VNP1_CHAR2_CLASS_LEVEL_REPORT.md` contains the requested status block, the glossary, the exact theorem list with dependencies, the dependency graph, the uniformity/freshness/flattening audit of the load-bearing theorem, the field scope, the build and axiom audits, the placeholder audit, and a section separating what is machine-checked here (all statements about the internal classes defined in this project) from the external literature synthesis (Valiant; Bringmann–Ikenmeyer–Zuiddam), which is stated only as prose and is not axiomatized. Support-two is recorded as OPEN and not attempted, and no P-vs-NP, VP-vs-VNP, or runtime consequence is claimed. No novelty claim is made.

# Summary of changes for run 028211d9-54c6-4b42-90ac-7709bd590115
Continued from the saved workspace without restarting or rewriting existing proofs.

**Checkpoint validation (narrow builds).** `FormulaDegrees`, `MainCompiler` and `PathTheorem` compiled unchanged (PASS). `FieldCorollary` did **not** compile: its `GaloisField` regression statements assumed a `Fintype (GaloisField 2 n)` instance that Mathlib does not provide (only `Finite`, with `GaloisField.card` stated via `Nat.card`). Repair: the GF(4)/GF(8)/GF(16) statements were restated with `Nat.card`, a `Nat.card` form of the general corollary was added (`exists_tau_of_two_lt_natCard`, `Formula.has_supportThree_representation_of_natCard_gt_two`), plus a direct GF(8) instance `Formula.has_supportThree_representation_GF8`. The `Fintype.card` version `Formula.has_supportThree_representation_of_card_gt_two` is unchanged and now builds.

**Frozen, sorry-free results.** The main compiler theorem `Formula.has_supportThree_affineHypercubeRepresentation` (representation of `f.eval`, support ≤ 3, `numAux ≤ 44·size f`, `numFactors ≤ 84·size f`, under `[Field F] [CharP F 2]` and `τ ≠ 0`, `τ ≠ 1`) and the path-selector theorem `PathGraph.path_selector_iff` (selector value 1 ⟺ the selection is exactly the edge set of a directed source→sink walk, with pair exclusions retained at source and sink and acyclicity supplied by the rank witness) are both verified; constants were not touched.

**New in this run.** `RegressionTests.lean` with four firewall theorems: τ = 0 and τ = 1 genuinely break the identity; `(∑ A)(∑ B) ≠ ∑ A·B` so gadget auxiliaries must be fresh; an unused summed Boolean variable annihilates the sum in characteristic two; and an edge labelled `x₁+x₂+x₃` yields a gadget factor of support 5, so support ≤ 3 is false for unrestricted ABPs. `OpenOwners.lean` records banked / open / scope-dead items — in particular that this is a representation theorem with no evaluator and no P-vs-NP, VP = VNP, or runtime consequence, and that the class-level and F₂ literature statements are neither formalized nor axiomatized. `AxiomAudit.lean` runs `#print axioms` on all principal theorems; every one reports only `propext`, `Classical.choice`, `Quot.sound`.

**Integration.** The aggregator `RequestProject/AlgebraicComplexity/VNP1Char2.lean` now imports every module of the layer, and `RequestProject/Main.lean` imports the aggregator with a single line. A full `lake build` completes successfully (errors: none; only linter warnings). A search of the whole tree for `sorry`, `admit`, `axiom`, `native_decide`, `opaque`, `unsafe`, `@[implemented_by]` returns only two matches, both inside documentation comments stating that no axiom is declared. The report `VNP1_CHAR2_ARISTOTLE_FORMALIZATION_REPORT.md` contains the requested status block, the theorem list, the repairs, the firewall tests, the build and axiom audits, and the strict final verdict. All work is committed.